# All variables have safe defaults - no manual input required.
# Copy to terraform.tfvars and override only what you need.
# terraform apply -auto-approve runs without any prompts.

aws_region          = "us-east-1"
environment         = "production"
project_name        = "cloud-intrusion-platform"
dynamodb_table_name = "intrusion-events"
